// removed in v1 cleanup — see git history. Compensation-line tests moved to
// test/compensation.test.mjs (core/compensation.mjs itself is unchanged).
import { test } from 'node:test';

test('removed', () => {});
