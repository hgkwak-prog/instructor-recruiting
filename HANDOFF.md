# 인수인계 — 재기획 세션용

2026-09-04. 코딩 중단. 이 문서는 새 세션에 붙여넣어 맥락을 넘기기 위한 것이다.

## 원래 하려던 일

PDF/HTML/MD로 된 커리큘럼·제안서를 받아 → 핵심만 추려 → 슬랙과 커리어데이에 강사 구인 공고를 게시한다.

## 지금 있는 것

모듈 약 30개, 테스트 291개. 파싱·추출·조립·게시 경로는 다 있고 테스트는 통과한다.
**단, 실제 모델 호출이 안 된다** (아래 참조).

## 왜 중단했나

기능을 추가하는 방식으로 문제를 풀어와서 핵심 대비 코드가 비대해졌다.

| | 무엇 |
|---|---|
| 꼭 필요 | 파서(PDF/HTML/MD) · 추출 · 공고 조립 · 슬랙 게시 |
| 방어 가능 | 승인 게이트(잘못 나가면 실제 피해) · 운영조건 입력(커리큘럼에 없는 값) |
| 덧붙은 것 | 검산기 · publish-guard · HTML 리포트 · 커리어데이 자동화 · 리마인더 · 호출 예산 · run 아티팩트 · 시트 |

세 번째 줄이 전체의 절반 이상이다.

구체적인 예: "검증이 너무 빡세다"는 지적에 검사기를 **덜어내는** 대신 운영조건 모달 + publish-guard + override 로직을 **새로 만들었다.** 강사료 빈칸에는 `core/compensation.mjs`를 만들었다.

## 막혀 있는 것

모델 호출이 `Reached maximum number of turns (1)`로 실패한다.

`adapters/llm/claude-agent.mjs`의 `ISOLATION_OPTIONS`에 `maxTurns: 1`이 걸려 있다. SDK 정의상 턴은 **API 왕복 횟수**이고, 구조화 출력에는 자체 재시도가 있다(`error_max_structured_output_retries`). 재시도가 두 번째 왕복이 되면서 막히는 것으로 **추정**되나 확정하지 않았다. 출력 잘림일 가능성도 남아 있다.

`maxTurns: 1`은 에이전트 루프를 막으려고 넣었는데, 그건 `allowedTools: []`가 이미 보장한다. 덤으로 건 안전장치가 정상 동작을 막은 사례다.

진단: `maxTurns`만 4로 올려 같은 프롬프트를 보내고 `num_turns` / `stop_reason`을 본다.

## 오늘 한 일 (커밋 안 됨)

규칙의 출처를 하나로 모았다. 24개 필드의 추출 기준을 `schemas/`의 각 필드 `description`으로 옮기고, SKILL.md에는 한 필드에 붙일 수 없는 규칙만 남겼다(5,279자 → 777자).

- `skills/jd-writer/` → `skills/jd-fact-extraction/`로 이름 변경. Cowork 계정 스킬 `jd-writer`(본문 작성)와 이름이 겹쳐 혼동을 낳았다. **둘은 하는 일이 반대다** — 계정 스킬은 JD 본문을 쓰고, 레포 것은 사실만 추출한다.
- `scripts/gen-schema.mjs`의 `fact()`가 `description`을 필수 인자로 받는다. 설명 없이 필드를 추가할 수 없다.
- run 디렉터리에 `schema.json`을 남긴다. 항목별 지시가 `outputFormat`으로 가므로 `prompt.txt`만 보면 절반이다.
- 모달에 마감 시각(timepicker) 추가.

`git status`로 확인할 것. 커밋하지 않았다.

## 재기획 때 정할 것

1. 어디까지가 v1인가. 위 3분류에서 "덧붙은 것"을 얼마나 버릴 것인가.
2. 커리어데이 자동화를 유지할 것인가. Playwright + 호스트 전용이라 비용이 크고, 폼 채우기는 끝까지 검증된 적이 없다.
3. 승인을 슬랙 버튼으로 유지할 것인가, 더 단순한 것으로 바꿀 것인가.
4. 구인 실행 트리거를 무엇으로 할 것인가 (아직 미정).

## 실행 전 주의

- `data/recruitment.sqlite`에 시운전 행이 섞여 있다. 실사용 전에 옆으로 치울 것.
- 실 워크스페이스용 슬랙 토큰 미발급. 현재 토큰은 테스트 채널용이다.
