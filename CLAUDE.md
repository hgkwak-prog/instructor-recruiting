# 강사 구인 자동화 — 프로젝트 메모리

이 레포에서 작업하는 모든 세션(계정·조직이 바뀌어도)이 참고해야 할 결정과 제약을 모아둔다.
현재 상태와 다음 할 일은 `HANDOFF.md`를 먼저 볼 것 — 이 파일은 "왜 이렇게 됐는가"를 담는다.

## 이 레포의 위치

사용자가 그리는 시스템은 교육 사업 전체 라이프사이클(문의 → 제안 → 강사 섭외 → 교육 →
사후관리)이다. 이 레포(`instructor-recruiting`)는 그중 **강사 구인** 한 조각만 다룬다.
전체로 착각하지 말 것.

목적: 공고 작성 자동화가 아니라, 채용 채널을 "강사 풀에 전화 돌리기"에서 "슬랙 공고"로
전환하는 것. 규모는 월 15건 수준.

**이전 레포 이름 주의**: `assistant-instructor-recruiting-agent`, `slack-recruiting-bot`은
2026-08-24 통합 이전의 구 레포다. 지금은 존재하지 않는 설계(오픈클로, Claude CLI 서브프로세스
직접 호출, Gemini/OpenAI API 등)를 언급하는 옛 메모가 어딘가 남아있다면 전부 무시하고 이
레포(`instructor-recruiting`)의 현재 코드를 기준으로 판단할 것.

## 핵심 아키텍처 원칙 (제일 중요, 절대 깨지 않을 것)

**모델은 사실만 추출하고, 공고 본문은 코드가 조립한다.**

- `core/render/job-post.mjs`가 facts로 JD를 조립한다. 날짜·요일·시간 합계·고정 문구는
  모델이 아니라 코드가 만든다.
- 스키마(`schemas/recruitment-result.schema.json`, `scripts/gen-schema.mjs`가 생성)가
  본문 문자열 자체를 필드로 받지 않는다 — 모델이 본문을 직접 쓸 길이 없다.
- 프롬프트 규칙의 출처는 스키마 필드 `description` 하나로 모은다 (`scripts/gen-schema.mjs`의
  `fact()`가 `description`을 필수로 요구). 한 필드에 못 붙는 규칙만 `skills/`에 남긴다.

## JD 비공개 정책 — 추출은 하되 게시는 안 함

공고는 대외 공개 문서다. 아래는 정확히 추출해 내부적으로는 갖고 있되 **공고 본문에는 안 낸다.**

| 항목 | 이유 |
|---|---|
| 일차별 커리큘럼 설계(`curriculumOutline`) | 우리가 파는 상품. 경쟁사가 베낄 수 있음 |
| 고객사명 | 대부분 비공개 계약. 익명 라벨(A사)조차 안 씀 |
| 강사료 금액 | 원문 금액 = 고객사 예산이지 강사에게 제시할 금액이 아님. 항상 `______`로 빈칸 |
| 정확한 주소 | `서울 성동구 인근` 수준으로 일반화 (통근 판단용) |

**교육 목표(`objectives`)는 공개한다** (2026-09 정정 — 예전엔 비공개였다). 실제 슬랙
게시글 5건을 보고 확인함: "무엇을 갖추게 되는가"는 커리큘럼 설계 자체가 아니라 지원자가
자기 역량과 맞는지 판단하는 정보라서 공개해도 된다는 결론.

## JD 본문 포맷 — 실제 사례 기준으로만 고칠 것

`core/render/job-post.mjs`는 2026-09 실제 슬랙 게시글 5건을 기준으로 다시 맞췄다.
헤더 `*[역할 모집] 과정명*`, 섹션 이모지(📌🎯📖✅💰), 모든 섹션 불릿+줄바꿈 통일
(강사료 줄 포함 — 처음엔 강사료만 불릿이 빠져 있어서 편집 중 정규식이 못 찾는 버그가
났었다).

**포맷을 고칠 때는 예시부터 확보하고 코드를 맞출 것.** 거꾸로(포맷을 먼저 정하고 나중에
사례로 검증)는 하지 말 것 — 실사용에서 여러 번 뒤집혔다(강사료 줄 불릿 누락, 교육목표/
주요내용을 문장 개수 안 가리고 한 줄로 join한 것 등, 전부 예시 없이 짰다가 실사용에서 깨짐).

## 자격 조건 정책

- **물류 조건**(일정 참여·출근·근무시간 가능 여부)은 `core/derive.mjs`가 확정 사실에서
  **코드로** 생성한다. 모델에 맡기지 않는다(표현이 흔들림).
- **역량 조건**은 모델이 작성한다. 원문에 없으면 제안을 허용한다 — 자격 조건 없는 공고는
  지원자가 판단할 수 없기 때문. `{ text, sourced }` 구조로 원문/제안 구분은 유지.
- **담당 업무**: 원문에 없으면 `derive.mjs`의 역할별 표준 세트를 쓴다(커리큘럼에는 보통
  안 적혀 있다).
- 자격·업무 각 줄은 45자 이내로 제한한다(테스트로 강제).

## v1 범위 결정 (2026-09-04 확정, 재논의 불필요)

코드가 "문제마다 기능을 덧붙이는" 방식으로 커져서(모듈 30개, 테스트 291개) 한 번 재기획했다.
결정:

1. 검산기(`core/verify.mjs`), `postable` 게이트, publish-guard, HTML 리포트, 리마인더,
   호출 예산, run 아티팩트, 시트 동기화 — **전부 제거.** 모든 생성 결과는 무조건
   검토대기로 들어가고 사람이 눈으로 보고 승인 여부를 정한다.
2. 커리어데이 자동화는 CLI 수동 트리거(`careerday-prepare`)로 축소. Playwright 자동
   폼 채우기는 없앴다.
3. 승인은 슬랙 버튼 하나로만 간다. CLI에는 승인 명령이 없다(공개 승인 지점이 둘이면
   같은 행을 두 곳에서 바꾸는 경합이 생긴다).
4. 구인 실행(파일을 봇에 올리는 것) 트리거는 사람이 수동으로 한다. 자동 감지 없음.
5. 운영조건(장소·일정·지원방법 등)은 파일 기본값이 아니라 건별로 슬랙 모달에서 사람이
   입력한다 — 출처 없는 값이 사실인 척 공고에 새는 걸 막기 위함.

새 기능을 논의할 때 이 목록에 없는 것은 기본적으로 v2 이후로 미룬다. 기능을 추가하기 전에
"이걸 안 만들면 무엇이 실제로 잘못되는가"를 먼저 묻는다 — 답이 "리포트가 시끄러워진다"
수준이면 만들지 않는다.

## 모델 호출 — 정액제 구독, 종량제 아님

`claude setup-token`으로 발급한 구독 OAuth 토큰(`CLAUDE_CODE_OAUTH_TOKEN`)을 쓴다.
`@anthropic-ai/claude-agent-sdk`를 자율 에이전트가 아니라 **격리된 구조화 추출기**로만
쓴다 — `adapters/llm/claude-agent.mjs`의 `ISOLATION_OPTIONS`: `allowedTools: []` +
`permissionMode: 'dontAsk'` + `settingSources: []` + `maxTurns: 4`.

- `maxTurns`는 원래 1이었다가 4로 올렸다. 작은 예시 커리큘럼은 1턴에 끝나서 문제없어
  보였지만, 실제 크기의 제안서 PDF는 구조화 출력 자체 재시도(SDK 내부)가 추가 왕복을
  필요로 해서 1로는 막혔다. **작은 예시로 통과했다고 원인을 확정하지 말 것 — 실제
  규모의 입력으로 재현해야 한다.**
- `ANTHROPIC_API_KEY`가 환경에 있으면 구독 토큰이 조용히 무시되고 종량 과금된다.
  `.env`에 이 키를 넣지 말 것.
- `settingSources: []`가 없으면 SDK가 로컬 `~/.claude/settings.json`/CLAUDE.md를 읽어
  개발자 로컬 설정이 추출 결과에 새어든다.
- SDK가 보고하는 비용(`estimatedCostUsd`)은 청구액이 아니다. 구독 인증에서 실제로
  차감되는 건 달러가 아니라 구독 사용 한도(5시간/주간 롤링 윈도우, 계정 전체 공유).

## 환경 제약 (실행 환경마다 다시 만날 것)

- **연결된 폴더는 OS 레벨에서 `unlink`/`rm`/`mv`를 막는다** (디바이스 브리지). bash 도구,
  `node:sqlite` 쓰기, git 내부 lock 파일 정리까지 영향받는다. git 커밋 자체는 성공하지만
  (내부적으로 atomic rename 사용) 그 뒤 `.git/index.lock`/`HEAD.lock`이 남아 다음 git
  명령을 막는다 — 사용자가 자기 터미널에서 `rm -f`로 지워줘야 풀린다. Claude가 커밋을
  제안할 때는 락 삭제 명령과 커밋 명령을 항상 같이 줄 것(사용자 요청, 2026-09-08).
  파일을 진짜로 지워야 할 때도 사용자가 직접 `rm`/`git rm`을 실행해야 한다.
- **bash 도구의 리눅스 샌드박스(`linux-arm64`)에는 Claude Agent SDK의 네이티브 바이너리가
  없다**(사용자 맥은 `darwin-arm64`). 실제 모델을 호출하는 명령은 Claude의 bash로 실행이
  안 되고, 사용자가 자기 맥 터미널에서 직접 돌려서 결과를 붙여넣어야 한다. `--dry-run`으로
  프롬프트 조립까지는 Claude 쪽에서 확인 가능.

## 자주 쓰는 명령

```bash
npm test                                          # 테스트 전체
npm run check                                     # 문법 체크 (파일 삭제 후엔 이 스크립트도 갱신할 것)
node apps/cli.mjs generate --source <파일>        # 추출 검증용, 운영조건 입력 없음
node apps/cli.mjs show --run-id <id>              # 조립된 JD 본문 확인
node apps/cli.mjs list                            # 진행 중인 작업 목록
npm run bot                                       # 슬랙 봇 실행 (.env에 SLACK_BOT_TOKEN/SLACK_APP_TOKEN 필요)
```
