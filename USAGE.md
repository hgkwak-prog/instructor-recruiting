# 강사 구인 봇 사용법

이 문서는 **강사 구인 담당자**를 위한 사용법입니다. 코드나 개발 얘기는 없습니다 —
슬랙에서 무슨 버튼을 누르고 뭘 입력하면 되는지만 담았습니다.

이 봇이 하는 일은 한 줄로: **커리큘럼 파일을 보내면, 강사 구인 공고 초안을
만들어 줍니다.** 초안은 사람이 검토·수정하고 승인 버튼을 눌러야만 실제로
게시됩니다 — 봇이 알아서 올리는 단계는 없습니다.

---

## 1단계 — 봇에게 커리큘럼 파일 보내기

봇과의 DM(다이렉트 메시지) 창에 커리큘럼·제안서 파일을 그대로 올리면 됩니다.
PDF, HTML, MD 형식을 읽을 수 있습니다.

파일을 올리면 봇이 바로 이렇게 답합니다.

> `커리큘럼.pdf` 받았습니다.
> **[운영사항 입력]** 버튼

아직 아무것도 처리되지 않은 상태입니다. **[운영사항 입력]** 버튼을 눌러야
다음 단계로 넘어갑니다.

---

## 2단계 — 운영사항 입력 (제일 중요한 단계)

버튼을 누르면 입력 창(모달)이 뜹니다. 여기서 입력하는 값이 **커리큘럼에 적힌
내용보다 우선**합니다. 커리큘럼은 제안서 단계에서 만든 참고 문서라, 실제
운영(장소·인원·일정)은 계약 과정에서 바뀌는 게 정상이기 때문입니다.

입력 항목은 다음과 같습니다.

| 항목 | 설명 |
|---|---|
| 모집할 역할 | 보조강사 / 주강사 |
| 교육 장소 | 실제 교육 장소. 공고에는 시·군·구까지만 나갑니다(상세 주소는 자동으로 잘림) |
| 모집 인원 | 몇 명을 뽑는지 |
| 교육 시간 | 하루 교육이 진행되는 시간대 (예: `10:00-17:00`) |
| 근무 시간 | 교육 시간과 다를 때만 입력 (준비·정리 시간이 붙는 경우) |
| 지원 방법 | 어떻게 지원받을지 (예: 이메일 회신) — **비우면 아무도 지원할 수 없으니 필수** |
| 모집 마감일 / 마감 시각 | 선택 항목. 안 넣으면 마감 없이 공고가 나갑니다 |
| 출장 | 타 지역 출장이면 체크 — 강사료 문구에 "출장비 포함"이 붙습니다 |
| **고객사 명시 요구사항** | 고객사가 확실히 요구한 조건만 한 줄에 하나씩 (예: "경력 5년 이상", "AWS 실무 경력 필수"). **커리큘럼에 안 적혀 있어도 실제로 그런 요구를 들었으면 여기에 적으세요.** 없으면 비워 두면 됩니다 |

> ⚠️ **왜 이 정도까지 사람이 직접 입력해야 하나요?**
> 커리큘럼에 없는 값을 봇이 추측해서 채우면, 근거 없는 내용이 마치 사실인
> 것처럼 공고에 나갈 수 있습니다. 특히 "경력 몇 년 이상" 같은 조건은
> 문서만 보고는 알 수 없는 정보라 일부러 자동으로 못 채우게 막아뒀습니다.
> 담당자가 실제로 아는 값만 넣어주시면 됩니다.

다 입력하고 **[공고 만들기]**를 누르면 다음 단계로 넘어갑니다.

---

## 3단계 — 처리 중 (진행 상황이 슬랙에 실시간으로 뜹니다)

제출 직후 슬랙 메시지 하나가 뜨고, 처리가 진행되는 동안 **그 메시지 하나가
계속 바뀌면서** 지금 어느 단계인지 보여줍니다.

```
⏳ 파일 읽는 중
⏳ 프롬프트 조립 중
⏳ 모델 세션 시작 (1차 시도)
⏳ 모델 응답 생성 중 (1차 시도, 2번째 응답 조각)
⏳ 공고 조립 중
✅ 처리 완료 — 아래에서 검토해 주세요.
```

- 대형 문서(실제 제안서 PDF 등)는 **1분 이상 걸릴 수 있습니다.** 화면이 안
  바뀐다고 멈춘 게 아닙니다.
- 만약 30초 넘게 정말 아무 변화가 없으면 "새 신호가 없습니다"라는 안내가
  한 번 뜹니다 — 이땐 실제로 오래 걸리는 상황이니 좀 더 기다려 보시고, 그래도
  안 되면 담당 개발자에게 알려주세요.
- 모델이 응답을 한 번에 제대로 못 만들면 자동으로 다시 시도합니다
  ("모델 재시도 중"이라고 표시됩니다). 이것도 정상 동작입니다.

---

## 4단계 — 검토 화면

처리가 끝나면 완성된 공고 초안이 통째로 올라옵니다. 이 화면에서 확인할 것들:

- **공고 본문 전체**가 그대로 보입니다 — 실제로 채널에 올라갈 문구와 동일합니다.
- `:no_entry: 확인 필요` 표시가 있으면 **아직 승인할 수 없는 상태**입니다.
  빈칸으로 남은 항목이 있다는 뜻이니, 아래 [강사료 입력·본문 편집] 버튼으로
  채워야 합니다.
- `:warning: 검토 사항`은 참고용 경고입니다 — 승인을 막지는 않지만 한 번
  읽어보시는 게 좋습니다.
- 화면 아래쪽에 이런 안내가 항상 붙습니다.

  > 고객사명·강사료 금액·정확한 주소·일차별 커리큘럼은 **의도적으로** 공고에서
  > 뺐습니다.

  이건 버그가 아니라 정책입니다. 이유는 6번 항목(자주 묻는 질문)을 참고하세요.

---

## 5단계 — 강사료 입력 및 본문 편집

**[강사료 입력·본문 편집]** 버튼을 누르면 편집 창이 뜹니다.

1. **강사료 입력 방식**을 고릅니다.
   - *시간당 단가*: 단가만 넣으면 확정된 총 시수를 곱해서 총액을 자동 계산합니다.
   - *총액 직접 입력*: 총 시수가 안 잡혀 있을 때 씁니다.
2. **금액**을 넣습니다. `100,000` 또는 `10만` 둘 다 됩니다.
   - ⚠️ 원문(커리큘럼)에 적힌 금액은 **고객사가 우리에게 준 예산**입니다.
     여기 넣는 건 **강사에게 실제로 제시할 금액**이니 헷갈리지 마세요.
3. **공고 본문**을 자유롭게 고칠 수 있습니다. 강사료 줄만은 위에서 입력한
   값으로 자동으로 바뀌니 직접 손댈 필요 없습니다.

**[반영]**을 누르면 검토 화면으로 돌아갑니다.

---

## 6단계 — 승인 또는 반려

검토 화면 맨 아래에 버튼 두 개가 있습니다.

- **게시 채널**: 기본 채널로 두거나, 다른 채널을 골라도 됩니다.
- **[승인하고 게시]**: 누르면 확인 팝업이 한 번 더 뜨고, 확인하면 바로
  선택한 채널에 게시됩니다. **이 버튼을 누르는 순간 실제로 공고가 나갑니다.**
- **[반려]**: 이번 건은 게시하지 않고 마무리합니다.

승인 명령은 이 버튼 하나뿐입니다. 다른 경로(터미널 명령 등)로는 게시를
승인할 수 없습니다 — 실수로 두 곳에서 같은 건을 승인하는 사고를 막기
위해서입니다.

---

## 자주 묻는 질문

**Q. 왜 일차별 커리큘럼(1일차 뭐, 2일차 뭐)이 공고에 안 나오나요?**
일차별 커리큘럼 설계는 우리가 파는 상품입니다. 공고는 외부에 공개되는
문서라, 경쟁사가 그대로 베낄 수 있는 내용은 일부러 뺍니다. (다만 "교육
목표"는 지원자가 자기 역량과 맞는지 판단할 정보라 공개합니다.)

**Q. 왜 강사료가 `______`로 빈칸으로 나오나요?**
커리큘럼에 적힌 금액은 고객사 예산이지 강사에게 제시할 금액이 아닙니다.
반드시 5단계(강사료 입력·본문 편집)에서 담당자가 직접 넣어야 합니다.

**Q. 고객사명이 왜 하나도 안 보이나요? 익명 라벨(A사)이라도 넣으면 안 되나요?**
대부분 계약이 비공개라, 익명 라벨조차 넣지 않습니다.

**Q. 얼마나 걸리나요?**
문서 크기에 따라 다릅니다. 작은 문서는 수십 초, 실제 제안서 PDF처럼 큰
문서는 1분 이상 걸릴 수 있습니다. 진행 메시지(3단계)를 보면서 기다려 주세요.

**Q. 운영사항을 잘못 입력했어요. 다시 할 수 있나요?**
네, 파일을 다시 보내서 처음부터 다시 진행하면 됩니다. 직전에 입력했던 값은
다음 입력 창에 기본값으로 다시 채워져 있습니다.

**Q. 승인했는데 실수였어요.**
게시 이후 취소 기능은 없습니다. 승인 버튼을 누르기 전, 검토 화면에서
충분히 확인해 주세요.

---

## 피드백 주시는 법

이 문서와 봇 사용 경험 관련 피드백은 **비즈팀 곽형곤에게 DM**으로 남겨주시면
됩니다. 특히 아래 내용이 궁금합니다.

- 운영사항 입력 창의 항목이 헷갈리지 않았는지 (특히 "고객사 명시 요구사항" 항목)
- 진행 메시지가 실제로 도움이 됐는지, 너무 잦거나 부족하진 않았는지
- 검토 화면에서 "확인 필요"/"검토 사항" 표시가 이해하기 쉬웠는지
- 공고 본문 자체가 예전 방식(강사 풀에 전화 돌리기)보다 나은지
